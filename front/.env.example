# URL base de la API. Si no se define, http.ts usa "/api/v1" relativo
# (mismo origen, útil detrás de un proxy en producción).
# Ejemplo en desarrollo: VITE_API_URL=http://localhost:3000/api/v1
VITE_API_URL=
